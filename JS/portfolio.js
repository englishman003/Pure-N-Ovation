//////////////////////////////////////////////
/*          Portfolio Page           */

    /* -------------  IMAGE CREATION  -------------- */

        ////////////////////////////////////////////////////
        // Project Big Picture Photos

            /* Pure N Ovation IMG Elements */

                // Images For Big Portfolio Picture Container
                    const purenovation = document.createElement(`img`);
                    const purenovation2 = document.createElement(`img`);
                    const purenovationAbout = document.createElement(`img`);
                    const purenovationSkills = document.createElement(`img`);

                    // Images For Sample Image Slider
                        const pure1 = document.createElement(`img`);
                        const pure2 = document.createElement(`img`);
                        const pure3 = document.createElement(`img`);
                        const pure4 = document.createElement(`img`);

            /* Hangman IMG Elements */

                // Images For Big Portfolio Picture Container
                    const hangman = document.createElement(`img`);
                    const hangman2 = document.createElement(`img`);
                    const hangman3 = document.createElement(`img`);
                    const hangman4 = document.createElement(`img`);
                    const hangman5 = document.createElement(`img`);
                    const hangman6 = document.createElement(`img`);

                // Images For Sample Image Slider
                    const hman1 = document.createElement(`img`);
                    const hman2 = document.createElement(`img`);
                    const hman3 = document.createElement(`img`);
                    const hman4 = document.createElement(`img`);
                    const hman5 = document.createElement(`img`);
                    const hman6 = document.createElement(`img`);

            /* Food Pantry IMG Elements */

                // Images For Big Portfolio Picture Container
                    const foodPantry = document.createElement(`img`);
                    const foodPantry2 = document.createElement(`img`);
                    const foodPantry3 = document.createElement(`img`);
                    const foodPantry4 = document.createElement(`img`);
                    const foodPantry5 = document.createElement(`img`);

                // Images For Sample Image Slider
                    const food1 = document.createElement(`img`);
                    const food2 = document.createElement(`img`);
                    const food3 = document.createElement(`img`);
                    const food4 = document.createElement(`img`);
                    const food5 = document.createElement(`img`);

            /* Food Pantry Video Elements */ 
                    const foodPantryVid = document.createElement(`video`);

            /* Droom IMG Elements */

                // Images For Big Portfolio Picture Container
                    const droom1 = document.createElement(`img`);
                    const droom2 = document.createElement(`img`);
                    const droom3 = document.createElement(`img`);
                    const droom4 = document.createElement(`img`);
                    const droom5 = document.createElement(`img`);

                // Images For Small Sample Slider
                    const droomONE = document.createElement(`img`);
                    const droomTWO = document.createElement(`img`);
                    const droomTHREE = document.createElement(`img`);
                    const droomFOUR = document.createElement(`img`);
                    const droomFIVE = document.createElement(`img`);

            /* Droom Video Elements */ 
                const droomVid = document.createElement(`video`);

            /* Pure N Ovation IMG Assignments */

                // Big Portfolio Image Assignments
                    purenovation.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot.png`;
                    purenovation2.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot-DigitalCreativity.png`;
                    purenovationAbout.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot-About.png`;
                    purenovationSkills.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot-Skills.png`;

                // Small Sample Picture Assignments
                    pure1.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot.png`
                    pure2.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot-About.png`
                    pure3.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot-Skills.png`
                    pure4.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot-DigitalCreativity.png`;

             /* Hangman IMG Assignments */

                // Big Portfolio Image Assignments
                    hangman.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot1.png`;
                    hangman2.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot2.png`;
                    hangman3.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot3.png`;
                    hangman4.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot4.png`;
                    hangman5.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot5.png`;
                    hangman6.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot6.png`;

                // Small Sample Picture Assignments
                    hman1.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot1.png`;
                    hman2.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot2.png`;
                    hman3.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot3.png`;
                    hman4.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot4.png`;
                    hman5.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot5.png`;
                    hman6.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot6.png`;

            /* Pure N Ovation Class Assignments */

                // Big Portfolio First Image
                    purenovation.classList.add(`sample-image`);
                    purenovation.classList.add(`image`);
                    purenovation.classList.add(`screenshot`);
                    purenovation.classList.add(`column_1-4`);
                    purenovation.classList.add(`row_1-4`);

                // Sample Image Class Assignments
                    pure1.classList.add(`sample-image`);
                    pure1.classList.add(`image`);
                    pure1.classList.add(`screenshot`);
                    pure1.classList.add(`column_1-4`);
                    pure1.classList.add(`row_1-4`);

                    pure2.classList.add(`sample-image`);
                    pure2.classList.add(`image`);
                    pure2.classList.add(`screenshot`);
                    pure2.classList.add(`column_1-4`);
                    pure2.classList.add(`row_1-4`);

                    pure3.classList.add(`sample-image`);
                    pure3.classList.add(`image`);
                    pure3.classList.add(`screenshot`);
                    pure3.classList.add(`column_1-4`);
                    pure3.classList.add(`row_1-4`);

                    pure4.classList.add(`sample-image`);
                    pure4.classList.add(`image`);
                    pure4.classList.add(`screenshot`);
                    pure4.classList.add(`column_1-4`);
                    pure4.classList.add(`row_1-4`);


            /* Hangman Class Assignments */

                // Big Portfolio First Image
                    hangman.classList.add(`sample-image`);
                    hangman.classList.add(`image`);
                    hangman.classList.add(`screenshot`);
                    hangman.classList.add(`column_1-4`);
                    hangman.classList.add(`row_1-4`);

                // Sample Image Class Assignments
                    hman1.classList.add(`sample-image`);
                    hman1.classList.add(`image`);
                    hman1.classList.add(`screenshot`);
                    hman1.classList.add(`column_1-4`);
                    hman1.classList.add(`row_1-4`);

                    hman2.classList.add(`sample-image`);
                    hman2.classList.add(`image`);
                    hman2.classList.add(`screenshot`);
                    hman2.classList.add(`column_1-4`);
                    hman2.classList.add(`row_1-4`);

                    hman3.classList.add(`sample-image`);
                    hman3.classList.add(`image`);
                    hman3.classList.add(`screenshot`);
                    hman3.classList.add(`column_1-4`);
                    hman3.classList.add(`row_1-4`);

                    hman4.classList.add(`sample-image`);
                    hman4.classList.add(`image`);
                    hman4.classList.add(`screenshot`);
                    hman4.classList.add(`column_1-4`);
                    hman4.classList.add(`row_1-4`);

                    hman5.classList.add(`sample-image`);
                    hman5.classList.add(`image`);
                    hman5.classList.add(`screenshot`);
                    hman5.classList.add(`column_1-4`);
                    hman5.classList.add(`row_1-4`);

                    hman6.classList.add(`sample-image`);
                    hman6.classList.add(`image`);
                    hman6.classList.add(`screenshot`);
                    hman6.classList.add(`column_1-4`);
                    hman6.classList.add(`row_1-4`);

            /* Pure N Ovation Video Creation */
                pureVid = document.createElement(`video`);

            /* Hangman Video Creation */
                hmanVid = document.createElement(`video`);
                
    /* -------------  CARD CREATION  -------------- */

    /* To create the cards you need to do the following:
            -> Create more card elements (Copy & Paste the ones on the portfolio-card.js)
            -> Initialize the new Card using the newly created and renamed elements.
            -> Use the newly created card's `createCard` method, with the renamed elements and the same classes as each of the other card's elements.
            -> Use other card methods to assign proper values to the card.
            -> Lastly, append said card's `section` to the small portfolio container.
        */

        //////////////////////////////////////////////////
        //   Portfolio Card Elements       //

            // Pure N Ovation Card Elements
                pureCard = document.createElement(`section`);
                pureHeader = document.createElement(`header`);
                pureDiv1 = document.createElement(`div`);
                pureDiv2 = document.createElement(`div`);
                pureCoverImage = document.createElement(`img`);
                pureInfo = document.createElement(`section`);
                pureP = document.createElement(`p`);
                pureFooter = document.createElement(`footer`);

            // Hangman Card Elements
                hangCard = document.createElement(`section`);
                hangHeader = document.createElement(`header`);
                hangDiv1 = document.createElement(`div`);
                hangDiv2 = document.createElement(`div`);
                hangCoverImage = document.createElement(`img`);
                hangInfo = document.createElement(`section`);
                hangP = document.createElement(`p`);
                hangFooter = document.createElement(`footer`);

        ///////////////////////////////////////
        //    Classes
            class Card {
                constructor(element, element2, element3, element4, element5, element6, element7 ){
                    this.section = element;
                    this.header = element2;
                    this.imageContainer = element3;
                    this.imageCollection = element4;
                    this.paragraph = element5;
                    this.info = element6;
                    this.tech = element7;
                }
                addClass(element, Class){
                    element.classList.add(Class);
                }
                createCard(element, element2, element3, element4, element5, element6, element7, element8, Class, Class2, Class3, Class4, Class5, Class6, Class7){
                    this.addClass(element, Class);
                    this.addClass(element2, Class2);
                    this.addClass(element3, Class3);
                    this.addClass(element4, Class4);
                    this.addClass(element6, Class5);
                    this.addClass(element7, Class6);
                    this.addClass(element8, Class7);
            
                    element.append(element2);
                    element.append(element3);
                    element3.append(element4);
                    element4.append(element5);
                    element.append(element6);
                    element6.append(element7);
                    element6.append(element8);
                }
            
                addProjectTitle(element, value){
                    element.textContent = value;
                }
                addProjectCoverImage(element, value, value2){
                    element.append(value);
                    value.src = value2.src;
                    
                };
            
                addProjectInfo(element, value){
                    element.textContent = value;
                };
            
                addTechStack(element, value){
                    element.textContent = value;
                };
            }
        ///////////////////////////////////////
        //    Arrays

            /* Arrays */

                // Small Sample Slider Arrays
                    const pureArray = [];
                    const hmanArray = [];
                
                // Big Portfolio Picture Arrays
                    const purenovationBig = [];
                    const hangmanBig = [];

                // Traverse Picture Array
                    const projectsArray = [];

    ///////////////////////////////////////
    //    Functions
        function pushMedia(media, array){
            return array.push(media);
        }

        function pushProject(project, array){
            return array.push(project);
        }

    ///////////////////////////////////////
    //    Function Calls

        // Putting Images Together For Big Portfolio Picture Containter | Pure N Ovation
            pushMedia(purenovation, purenovationBig);
            pushMedia(purenovation2, purenovationBig);
            pushMedia(purenovationAbout, purenovationBig);
            pushMedia(purenovationSkills, purenovationBig);


        // Putting Images Together For Small Sample Slider | Pure N Ovation
            pushMedia(pure1, pureArray);
            pushMedia(pure2, pureArray);
            pushMedia(pure3, pureArray);
            pushMedia(pure4, pureArray);

        // Putting Images Together For Big Portfolio Picture Containter | Pure N Ovation
            pushMedia(hangman, hangmanBig);
            pushMedia(hangman2, hangmanBig);
            pushMedia(hangman3, hangmanBig);
            pushMedia(hangman4, hangmanBig);
            pushMedia(hangman5, hangmanBig);
            pushMedia(hangman6, hangmanBig);

        // Putting Images Together For Small Sample Slider | Hangman
            pushMedia(hman1, hmanArray);
            pushMedia(hman2, hmanArray);
            pushMedia(hman3, hmanArray);
            pushMedia(hman4, hmanArray);
            pushMedia(hman5, hmanArray);
            pushMedia(hman6, hmanArray);


    /* -------------  CLASS INSTANCES  -------------- */

        ////////////////////////////////////////////////////
        // Card Class

            const PURE = new Card(pureCard, pureHeader, pureDiv1, pureDiv2, pureInfo, pureP, pureFooter);
            const Hangman = new Card(hangCard, hangHeader, hangDiv1, hangDiv2, hangInfo, hangP, hangFooter);

            /* Card Method Usage */

                // Pure N Ovation

                    PURE.createCard(pureCard, pureHeader, pureDiv1, pureDiv2, pureCoverImage,  pureInfo, pureP, pureFooter, `portfolio-card`, `portfolio-card_header`, `portfolio-card_image`, `image`, `portfolio-card_info`, `portfolio-card_info-paragraph`, `portfolio-card_info-footer`);

                    PURE.addProjectTitle(pureHeader, `PureNOvation`);
                    PURE.addProjectCoverImage(pureDiv2, pureCoverImage, pureArray[0]);
                    PURE.addProjectInfo(pureP, `My personal experimental playground`);
                    PURE.addTechStack(pureFooter, `HTML5 | CSS3 | JavaScript | Vue.js | Node.js`);

                // Hangman

                    Hangman.createCard(hangCard, hangHeader, hangDiv1, hangDiv2, hangCoverImage,  hangInfo, hangP, hangFooter, `portfolio-card`, `portfolio-card_header`, `portfolio-card_image`, `image`, `portfolio-card_info`, `portfolio-card_info-paragraph`, `portfolio-card_info-footer`);

                    Hangman.addProjectTitle(hangHeader, `Hangman`);
                    Hangman.addProjectCoverImage(hangDiv2, hangCoverImage, hmanArray[0]);
                    Hangman.addProjectInfo(hangP, `Create your own word, and others can guess!`);
                    Hangman.addTechStack(hangFooter, `HTML5 | CSS3 | JavaScript | SCSS`);




    /*--------------  Page Placement  ---------------*/

            /* Page Elements */

                const projectTitle = document.getElementsByClassName(`project-bio-container_header_project-title`)[0];

                const bigProjectPicture = document.getElementsByClassName(`big-project-picture`)[0];
                const sampleMediaContainer = document.getElementsByClassName(`project-media-container_images`)[0];

                const bigPortfolio = document.getElementsByClassName(`big-portfolio-viewport`)[0];
                const bigLeftDoor = document.getElementsByClassName(`big-portfolio-viewport_door-left`)[0];
                const bigRightDoor = document.getElementsByClassName(`big-portfolio-viewport_door-right`)[0];
                const bigPortfolioClose = document.getElementsByClassName(`project-bio-container_header_close`)[0];
                const smallPortfolio = document.getElementsByClassName(`small-portfolio-container`)[0];
                const portfolioCards = document.getElementsByClassName(`portfolio-card`);
                const sampleImages = document.getElementsByClassName(`sample-image`);
                const portfolioProjectLinks = document.getElementsByClassName(`project-bio-container_links_link`);

                const traversePrevious = document.getElementsByClassName(`project-bio-container_project-traverse_previous`)[0];
                const traverseNext = document.getElementsByClassName(`project-bio-container_project-traverse_next`)[0];
                const traversePPrevious = document.getElementsByClassName(`previous-project`)[0];
                const traversePNext = document.getElementsByClassName(`next-project`)[0];

                const arrowRight = document.getElementsByClassName(`arrow-right-container`)[0];
                const arrowLeft = document.getElementsByClassName(`arrow-left-container`)[0];

        /* Card Placement */
        smallPortfolio.append(PURE.section);
        smallPortfolio.append(Hangman.section);

        /* Project Compilation */
        pushProject(PURE, projectsArray);
        pushProject(Hangman, projectsArray);        

    /*--------------  Page Functionality  ---------------*/

        /////////////////////////////////////////////
        //  Interaction Variables
        let currentProject = 0;
        let currentPicture = 0;
        let translationStart = 16;
        let translationMultiplier = 1;
        let upOrDown;



        /////////////////////////////////////////////
        //  Interaction Functions

            /* Removing All Image Nodes */
                function removeImageChildren(container){  // A function to remove all of the image child nodes of the imageContainer.
                    for(let i = container.childNodes.length - 1; i > 0 ; i--){
                        container.removeChild(container.childNodes[i]);
                    }
                }

            /* Revealing The Small Portfolio Card Container */
                function revealSmallPortfolio(element){
                    smallPortfolio.style.display = `flex`;
                }

            /* Revealing The Big Portfolio Viewport */
                function revealBigPortfolio(element){
                    bigPortfolioClose.style.display = `flex`;
                }

                function calculateTranslation(number , multiplier){
                   return number - (number * multiplier);
                }

                function calculateTranslation2(number){
                    return number += 16;
                }

                function changeSign(cb){
                    return (cb * -1);
                }

                function translateImagesForward(){
                    upOrDown = 1;
                    if(upOrDown === 1){
                        let samples = Array.from(sampleImages);
                        calculateTranslation(currentPicture, 16);
                        samples.forEach((item) => {
                        item.style.transform = `translateX(${calculateTranslation(translationStart, currentPicture)}rem)`;
                    });
                    }
                }

                function translateImagesBackward(){
                    upOrDown = 0;
                    if(upOrDown === 0){
                        let samples = Array.from(sampleImages);
                        calculateTranslation(currentPicture, 16);
                        samples.forEach((item) => {
                        item.style.transform = `translateX(${calculateTranslation(translationStart, currentPicture)}rem)`;
                    });
                    }
                }

                function showNextImage(array){
                    if(currentPicture === array.length - 1){
                        let replacement = document.createElement((`img`));
                        replacement.src = array[currentPicture].src;
                        replacement.classList.add(`image`);
                        replacement.classList.add(`screenshot`);
                        replacement.classList.add(`column_1-4`);
                        replacement.classList.add(`row_1-4`);
                        bigProjectPicture.firstChild.nextSibling.replaceWith(replacement);
                        console.log(currentPicture);
                        alert(`No More Media Left`);
                    }
                    else if(currentPicture < array.length - 1){
                        currentPicture++;
                        let replacement = document.createElement((`img`));
                        replacement.src = array[currentPicture].src;
                        replacement.classList.add(`image`);
                        replacement.classList.add(`screenshot`);
                        replacement.classList.add(`column_1-4`);
                        replacement.classList.add(`row_1-4`);
                        bigProjectPicture.firstChild.nextSibling.replaceWith(replacement);
                        translateImagesForward();
                        let samples = Array.from(sampleMediaContainer.childNodes);
                        samples.shift();
                        samples.forEach((item) => {
                            item.style.opacity = .5;
                        });
                        samples[currentPicture].style.opacity = 1;
                        console.log(currentPicture);
                    }
                }

                function showPreviousImage(array){
                    if(currentPicture === 0){
                        let replacement = document.createElement((`img`));
                        replacement.src = array[currentPicture].src;
                        replacement.classList.add(`image`);
                        replacement.classList.add(`screenshot`);
                        replacement.classList.add(`column_1-4`);
                        replacement.classList.add(`row_1-4`);
                        bigProjectPicture.lastChild.replaceWith(replacement);
                        console.log(currentPicture);
                        alert(`No Previous Media`);
                    }
                    else if(currentPicture > 0){
                        currentPicture--;
                        let replacement = document.createElement((`img`));
                        replacement.src = array[currentPicture].src;
                        replacement.classList.add(`image`);
                        replacement.classList.add(`screenshot`);
                        replacement.classList.add(`column_1-4`);
                        replacement.classList.add(`row_1-4`);
                        bigProjectPicture.lastChild.replaceWith(replacement);
                        translateImagesBackward();
                        let samples = Array.from(sampleMediaContainer.childNodes);
                        samples.shift();
                        samples.forEach((item) => {
                            item.style.opacity = .5;
                        });
                        samples[currentPicture].style.opacity = 1;
                        console.log(currentPicture);
                    }
                }

                function openPure(){
                    currentProject = 0;
                    smallPortfolio.style.display = `none`;
                    setTimeout(revealBigPortfolio, 1200);
                    bigLeftDoor.style.transform = `translateX(-100%)`;
                    bigRightDoor.style.transform = `translateX(100%)`;
                    removeImageChildren(bigProjectPicture);
                    removeImageChildren(sampleMediaContainer);
                    bigProjectPicture.append(purenovationBig[0]);
                    for(let i = 0; i < pureArray.length; i++){
                        sampleMediaContainer.append(pureArray[i]);
                    }
                    let previousImage = document.createElement(`img`);
                    let nextImage = document.createElement(`img`);
                    previousImage.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot.png`;
                    nextImage.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot1.png`;
                    previousImage.classList.add(`screenshot`);
                    nextImage.classList.add(`screenshot`);
                    traversePrevious.firstChild.nextSibling.replaceWith(" ");
                    traversePPrevious.textContent = `No Previous Project`;
                    traversePNext.textContent = `Next Project`;
                    traverseNext.firstChild.nextSibling.replaceWith(nextImage);
                    traversePrevious.append(traversePPrevious);
                    traverseNext.append(traversePNext);
                    projectTitle.textContent = `Pure N Ovation`;
                    sampleMediaContainer.firstChild.nextSibling.style.opacity = 1;
                    portfolioProjectLinks[0].setAttribute(`href`, `index.html`);
                    portfolioProjectLinks[1].setAttribute(`href`, `https://github.com/englishman003/Pure-N-Ovation`);
                    
                }

                function openHang(){
                    currentProject = 1;
                    smallPortfolio.style.display = `none`;
                    bigPortfolio.style.display =`flex`;
                    bigLeftDoor.style.transform = `translateX(-100%)`;
                    bigRightDoor.style.transform = `translateX(100%)`;
                    removeImageChildren(bigProjectPicture);
                    removeImageChildren(sampleMediaContainer);
                    bigProjectPicture.append(hangmanBig[0]);
                    for(let i = 0; i < hmanArray.length; i++){
                        sampleMediaContainer.append(hmanArray[i]);
                    }
                    let previousImage = document.createElement(`img`);
                    let nextImage = document.createElement(`img`);
                    previousImage.src = `Design_Files/Project-Screenshots/PureNOvation-Screenshot.png`;
                    nextImage.src = `Design_Files/Project-Screenshots/Hangman-Media/Hangman-Screenshot1.png`;
                    previousImage.classList.add(`screenshot`);
                    nextImage.classList.add(`screenshot`);
                    traversePrevious.firstChild.nextSibling.replaceWith(previousImage);
                    traverseNext.firstChild.nextSibling.replaceWith(" ");
                    if(currentProject < projectsArray.length - 1 && currentProject > 0){
                        traversePPrevious.textContent = `Previous Project`;
                        traversePNext.textContent = `Next Project`;
                        traversePrevious.append(traversePPrevious);
                        traverseNext.append(traversePNext);
                    }
                    else if(currentProject === projectsArray.length - 1){
                        traversePNext.textContent = `No Next Project`;
                        traversePPrevious.textContent = `Previous Project`;
                        traverseNext.append(traversePNext);
                        traversePrevious.append(traversePPrevious);
                    }
                    projectTitle.textContent = `Hangman`;
                    sampleMediaContainer.firstChild.nextSibling.style.opacity = 1;
                    portfolioProjectLinks[0].setAttribute(`href`, `https://hangman-eta.now.sh/`);
                    portfolioProjectLinks[1].setAttribute(`href`, `https://github.com/englishman003/Hangman`);
                }

            /* Close Big Portfolio Viewport Event Listener */
                bigPortfolioClose.addEventListener(`click`, () => {
                    bigLeftDoor.style.transform = `translateX(0)`;
                    bigRightDoor.style.transform = `translateX(0)`;
                    setTimeout(revealSmallPortfolio, 1100);
                });

                function projectOpen(number){
                    if(number === 0){
                        openPure();
                    }
                    else if(number === 1){
                        openHang();
                    }
                }

            /* Pure N Ovation Card Event Listener */
                PURE.section.addEventListener(`click`, () => {
                    openPure();
                });

            /* Hangman Card Event Listener */
                Hangman.section.addEventListener(`click`, () => {
                    openHang();
                });

                traversePrevious.addEventListener(`click`, () => {
                    currentProject--;
                    if(currentProject < 0){
                        traversePPrevious.textContent = `No Previous Project`;
                        alert(`No Previous Projects Available!`);
                        currentProject = 0;
                    }
                    else if(currentProject === 0){
                        openPure();
                    }
                    else if(currentProject === 1){
                        openHang();
                    }
                });

                traverseNext.addEventListener(`click`, () => {
                    currentProject++;
                    if(currentProject >= projectsArray.length){
                        alert(`No Next Projects Available!`);
                        currentProject = projectsArray.length - 1;
                    }
                    else if(currentProject === 1){
                        projectOpen(currentProject);
                    }
                });

                arrowLeft.addEventListener(`click`, () => {
                    if(currentProject === 0){
                        showPreviousImage(pureArray);
                    }
                    if(currentProject === 1){
                        showPreviousImage(hmanArray);
                    }
                });

                arrowRight.addEventListener(`click`, () => {
                    if(currentProject === 0){
                        showNextImage(pureArray);
                    }
                    if(currentProject === 1){
                        showNextImage(hmanArray);
                    }
                });
